spring:
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://localhost:3306/Telecom?useUnicode=true&characterEncoding=utf8
    username: root
    password: 20001218

server:
  port: 8080

mybatis:
#  mapper-locations: classpath:mapper/*.xml
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
